<div class="container top-bar home-border", style="color blue">
    <div class="attached">
      <div class="name" onclick="void(0);">
        <%= link_to "GradScore", root_path %>
      </div>
 
      <ul class="right">
        <% if current_user %>
          <li>
            <%= link_to "New Assessment", new_student_path %>
          </li>
          <li>
            <%= link_to "Search for a Grad Assessment", students_path %>
          </li>
        <% end %>
       
        <% if current_user %>
          <li>
            <%= link_to "Sign Out", destroy_user_session_path, :method => 'delete'  %>
          </li>
        <% else %>
          <li>
            <%= link_to "Sign In", new_user_session_path %>
          </li>
          <li>
            <%= link_to 'Sign Up', new_user_registration_path %>
          </li>
        <% end %>


      </ul>
    </div>
  </div>